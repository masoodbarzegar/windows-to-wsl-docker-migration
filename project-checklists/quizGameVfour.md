# Project Migration Checklist — quizGameVfour

Project name: quizGameVfour
Source (Windows): `E:\LocalDev\Laravel\quizGameVfour`
Destination (WSL): `/home/masoud/personal/laravel/quizGameVfour`

Laravel 12 + React 19 + MySQL 8.0. Same Docker scaffolding family as `quizGameVtwo`/
`quizGameVthree`, most-evolved of the three (games/questions/game_sessions schema, its own
GitHub remote). Branch `master`, 13 files with uncommitted working-tree changes at time of
migration (preserved as-is).

## Duplicate resolution

`quizGameVfour-1` and `quizGameVfour-2` (plus `quizGameVfour-1.rar`/`quizGameVfour.rar`) exist
alongside the main project, per the original audit's flag. Resolved via git evidence rather than
file diffing: both `-1` and `-2` are at the exact same commit (`b633ea6`, 2025-06-02), while the
main copy is ahead at `a4e0097` (2025-06-13) with a GitHub remote configured
(`github.com/masoodbarzegar/Quiz-game-laravel.git`). Confirms `-1`/`-2` are superseded snapshots,
not divergent forks. Migrated only the main copy; `-1`, `-2`, and both `.rar` archives left
untouched.

## Audit correction

The original audit listed "DB dir not found on disk at this path (app-only stack)" for this
project. This was wrong — there is a real bind-mounted MySQL datadir with live data, and it
contains **two** schemas: `quiz` (production-like data) and `quiz_testing` (schema-only, no
rows — confirmed as a real, intentionally-used test database via `phpunit.xml`'s
`TEST_DB_DATABASE=quiz_testing`, not an accidental leftover). Recorded in `migration-plan.md`
under "Audit corrections."

## 1. Discovery

- No running/stopped container collisions (all 4 containers already `Exited`).
- Dockerfile diverges from `quizGameVtwo`/`quizGameVthree`: adds Xdebug (configured for
  `host.docker.internal`, a Windows-Docker-Desktop convention that also works under WSL2's Docker
  integration) and installs `phpunit/phpunit` globally via Composer. Neither affects migration —
  Xdebug is a dev-tool connection concern, not an app-functionality blocker.
- `mysql:8.0` image digest (`sha256:50b5ee0656a2...`) already validated 4x prior in this
  environment.
- `docker/mysql/data/ibdata1` mtime was very recent (today) at preflight time, and
  `quiz-mysql-4`/`quiz-phpmyadmin-4` showed "Exited (0) about an hour ago" rather than the
  months-old exit times of the other two containers in the stack — something started and stopped
  this specific container recently. Not investigated (out of current scope), noted for awareness
  only; did not affect the migration.

## 2. Backup — complete

- [x] Physical evidence backup: `~/docker-audit/project-checklists/quizGameVfour-backups/dbdata-copy`
      — verified: structure matches source (34/34 entries), all protected directories present
      (`mysql`, `performance_schema`, `sys`, `quiz`, `quiz_testing`, `#innodb_redo`,
      `#innodb_temp`), source mtimes confirmed unchanged before/after.
- [x] `lower_case_table_names` compatibility probe: identical mismatch to `quizGameVtwo`/
      `quizGameVthree` — no divergence.
- [x] NTFS-hosted recovery working copy: `/mnt/e/Dev-Backups/quizGameVfour/dbdata-recovery-copy`.
- [x] Isolated recovery container started cleanly.
- [x] Logical dump covering **both** schemas: `quizGameVfour-logical-20260716-231128.sql` — 26
      `CREATE TABLE` statements (13 tables × 2 schemas), verified row counts (`quiz`: clients=2,
      game_sessions=22, games=1, migrations=15, questions=50, sessions=5, users=6;
      `quiz_testing`: migrations=15, all data tables empty as expected for a test DB).
- [x] Temporary recovery container removed; original Windows source and evidence backup both
      reconfirmed untouched (mtime check) after use.

## 3. Copy — complete

- [x] `rsync` source → `/home/masoud/personal/laravel/quizGameVfour`, excluding only
      `src/vendor/`, `src/node_modules/`, and the old `docker/mysql/data/`. Git branch, commit,
      and all 13 uncommitted changes confirmed identical to source after copy.
- [x] **New rsync exit-23 case (not the known DB-permission pattern)**: partial transfer caused by
      `src/storage/framework/testing/disks` (root:root, mode 700) — Laravel's `Storage::fake()`
      test-scaffold directory, not application data. Verified via root-mounted container read:
      contains only an empty `disks/public` folder and a `.gitignore`, zero actual files. Treated
      as acceptable for the same underlying reason as the established DB-permission pattern
      (nothing of substance skipped, auto-regenerated by Laravel's test runner) — a new instance
      of the same category of judgment, not a new exception invented for this project.

## 4. Compose normalization — complete

- [x] `compose.yaml`: `name: quizgamevfour_wsl`, same 4-service shape as the other two
      `quizGame*` migrations.
- [x] `.env.docker` / `.env.docker.example`: conflict-free ports chosen against all configured
      WSL migrations — `WEB_HTTP_PORT=8002`, `APP_FPM_PORT=9005`, `VITE_DEV_PORT=5179`,
      `PHPMYADMIN_PORT=8083`.
- [x] **Applied proactively (not rediscovered)**: the `mysql` network alias fix on the `db`
      service, since this project has the same `DB_HOST=mysql` / `SESSION_DRIVER=database`
      combination that broke `quizGameVthree`. Confirmed working via `getent hosts mysql` before
      and after restart — a known project-specific requirement applied directly, per the current
      instruction to treat prior migrations' genuine findings as reusable knowledge without
      reopening the framework itself.
- [x] Fresh named volume `quizgamevfour_wsl_dbdata` created; both schemas restored from the dump
      (the compose `MYSQL_DATABASE` env var only creates `quiz` on a truly fresh init;
      `quiz_testing` is created by the dump's own `CREATE DATABASE` statement); content
      independently verified against pre-restore counts for both schemas.

## 5. Validation — complete

- [x] `composer install` — clean; `inertiajs/inertia-laravel` already properly declared (same as
      `quizGameVthree`, no PHP-side dependency gap here).
- [x] `npm install` — clean, no missing-package errors this time.
- [x] **Genuine project-specific bug found and fixed**: `resources/js/Pages/Contact.test.jsx`
      contained a duplicated `import Contact from '@/Pages/Contact';` statement (lines 4 and 39 —
      a copy-paste artifact from the developer's own iterative editing, confirmed by reading the
      file directly). This broke Vite's dependency pre-bundling scan for the **entire** app (not
      just this test), since Inertia's page-resolution glob crawls all `Pages/**/*.jsx` files
      including this test file. Fixed by removing the later duplicate line — a one-line,
      unambiguous correction, not an application-logic change (vitest hoists `vi.mock()` calls
      automatically regardless of import position, so import order here was never load-bearing).
- [x] Storage/bootstrap-cache permission fix applied (same known bind-mount pattern as every
      prior MySQL migration).
- [x] Homepage, `/about`, `/contact`, `/login` all HTTP 200. `/games` HTTP 200 with confirmed
      real DB-backed content (`"name":"Time-based English Quiz"`, matching the restored `games`
      row). Auth-gated `/play/{slug}/start` correctly 302-redirects unauthenticated.
- [x] No new errors in `laravel.log` from today's traffic (22 total `ERROR` lines in the log are
      all historical, predating migration).
- [x] `mysql` network alias confirmed resolving via `getent hosts` both before and after restart.
- [x] Restart validated (`docker compose restart`): `db` healthy, homepage and `/games` both
      still 200 with the same restored game data confirmed present via direct query
      (`games`=1, `users`=6), `mysql` alias still resolving. Vite dev server (manually-started
      background process) needed a manual restart after the container restart — same known
      characteristic as the other two `quizGame*` migrations.

## Notes / deviations

- Stack left running as instructed; no cleanup performed.
- Original Windows source (main copy), physical evidence backup, and NTFS recovery copy all
  confirmed untouched throughout. `quizGameVfour-1`, `quizGameVfour-2`, and both `.rar` archives
  were not touched at all (correctly excluded from migration per the duplicate-resolution
  finding above).
- One code change was made to the WSL copy's own source: removed a duplicate import line in
  `Contact.test.jsx` (see Validation section) — a pre-existing bug, not migration-caused, fixed
  as the smallest possible adjustment to unblock the dev server.
